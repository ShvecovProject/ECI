(function () {
    function apiService(rootScope, state) {
      
    }

    angular.module("EZECastleIntegrationSPA.Common")
        .factory("ApiService", ["$rootScope", "$state", apiService]);
})();